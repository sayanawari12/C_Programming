#include <stdio.h>
#include <conio.h>

int main() 
{ 
    char ch;
    puts("Enter one character: ");
    ch = getche();   
    puts("\nYou have given: ");
    putchar(ch);
    getch(); 
    return 0;
}
